{{!< default}}

<header role="banner">
    <div class="bannerfeatureimage" {{#if @blog.cover}}style="background-image: url({{@blog.cover}})"{{/if}}>
		<div class="text_feature">
			<h1>{{@blog.title}}</h1>
			<hr>
		</div>
    </div>
</header>

{{> "loop"}}

{{> "footer"}}